[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.single"

[deploy]
# Health check via Next.js proxy to FastAPI backend
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
numReplicas = 1
